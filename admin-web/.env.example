# Copy to .env.development / .env.production (both gitignored) and fill in.
# Vite only exposes vars prefixed VITE_ to client code.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
VITE_ENVIRONMENT=development
